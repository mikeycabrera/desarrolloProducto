import {Region} from '../clases/region';

export class Producto {
    id: number;
    nombre: string;
    imagen: string;
    precio: string;
    stock: number;
    cliente: string;
    region: Region;
}
